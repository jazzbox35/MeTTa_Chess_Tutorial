import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Play Chess",
}

export default function ChessPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">chess</div>
  )
}
